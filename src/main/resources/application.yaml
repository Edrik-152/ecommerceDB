spring:
  application:
    name: ecommerceDB
  datasource:
    url: jdbc:postgresql://localhost:5432/ecommerce
    username: postgres
    password: root


  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        formate-sql: true
